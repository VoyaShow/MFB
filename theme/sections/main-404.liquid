<section class="section section--cream">
  <div class="page-width center">
    <img src="{{ 'mfb-product-flower-box.png' | asset_url }}" alt="" width="420" height="420"
         style="max-width:320px;margin:0 auto 20px">
    <h1>This box is empty</h1>
    <p class="lede" style="max-width:520px;margin:0 auto 26px">
      We cannot find that page — but we can find you some flowers.
    </p>
    <div class="cluster cluster--center">
      <a class="btn btn--lg" href="{{ routes.all_products_collection_url }}">Surprise Me!</a>
      <a class="btn btn--secondary btn--lg" href="{{ routes.root_url }}">Back home</a>
    </div>
  </div>
</section>
{% schema %}
{ "name": "404" }
{% endschema %}
